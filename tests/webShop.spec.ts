import {test} from "@playwright/test";
import { RegisterPage } from "../pages/tricentisShop/registerPage";
import { LoginPage } from "../pages/tricentisShop/loginPage";



test.describe('Webshop Tests', async() => {

    test("Register to webshop",async({page})=>{
        // const registerPage = new RegisterPage(page);
        const loginPage = new LoginPage(page);

    test
    })
})